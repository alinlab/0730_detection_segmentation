name: 0730_segmentation
channels:
  - defaults
dependencies:
  - python=3.7
  - pydot
  - graphviz
  - pip:
    - jupyterlab
    - matplotlib
    - tensorflow-gpu
    - tfds-nightly
prefix: https://raw.githubusercontent.com/alinlab/0730_detection_segmentation/master/seg_env.yml
